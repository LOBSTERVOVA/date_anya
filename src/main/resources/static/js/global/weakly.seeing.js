// Placeholder for global weekly UI helpers
// If you later add real logic, keep the public API stable to avoid breaking pages.
(function(){
  window.Weekly = window.Weekly || {
    noop: function(){},
  };
})();
